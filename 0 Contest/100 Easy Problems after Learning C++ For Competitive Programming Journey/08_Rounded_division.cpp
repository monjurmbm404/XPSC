// https://marisaoj.com/problem/9

#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    float a, b;
    cin >> a >> b;

    cout << fixed << setprecision(2) << a / b;

    return 0;
}